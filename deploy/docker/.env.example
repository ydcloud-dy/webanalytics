# WebAnalytics Docker Compose 环境变量
# 复制此文件到项目根目录: cp deploy/docker/.env.example .env

# 应用端口
APP_PORT=8080

# 时区
TZ=Asia/Shanghai

# JWT 密钥（生产环境务必修改）
JWT_SECRET=change-me-in-production

# ClickHouse 端口
CH_HTTP_PORT=8123
CH_NATIVE_PORT=9000

# ClickHouse 密码（留空则无密码）
CH_PASSWORD=
